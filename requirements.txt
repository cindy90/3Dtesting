# Core harness
requests>=2.31
PyYAML>=6.0

# Objective geometry analysis (the production-usability metrics)
trimesh>=4.0
numpy>=1.24
scipy>=1.10          # sparse ops used by trimesh proximity / normals
rtree>=1.0           # spatial index for ray / proximity queries

# Optional but recommended for robust glb/obj loading and boolean checks:
#   pip install "trimesh[easy]"
